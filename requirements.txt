# Phase 1 (MVP) - Required dependencies
notion-client>=2.2.1
feedparser>=6.0.11
PyYAML>=6.0.2
httpx>=0.27.2  # Also used for async HTTP requests in Phase 3
pytz>=2024.1
python-dateutil>=2.9.0
tenacity>=8.2.0
pydantic>=2.0.0
pydantic-settings>=2.0.0  # Configuration management
loguru>=0.7.0  # Logging
diskcache>=5.6.0  # Caching
langchain-core>=0.1.0  # Pipeline framework

# Phase 3 (Performance) - Async support
aiohttp>=3.9.0  # Async HTTP client for collectors

# Testing dependencies
pytest>=7.4.0
pytest-mock>=3.11.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0  # Async test support

# Phase 2 (LLM Enhancement) - Optional
# Uncomment when enabling LLM features
litellm>=1.0.0  # Unified LLM interface
# openai>=1.0.0  # Not needed if using litellm
# anthropic>=0.18.0  # Not needed if using litellm

